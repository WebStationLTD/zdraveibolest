export const metadata = {
  title: "Потвърдете имейла си",
  description: "Последна стъпка за достъп до вашия профил.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function VerifyEmailLayout({ children }) {
  return <>{children}</>;
}
